name: update-plugin-metadata

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    uses: ./.github/workflows/extract-plugin-metadata.yml
    with:
      package_spec: .
      auto_commit: false
